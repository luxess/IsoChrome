# IsoChrome Lite IDE 🚀

**IsoChrome Lite IDE** — сверхлегкий менеджер изолированных сессий Google Chrome на Batch. Скрипт запускает «чистые» окна браузера, умеет создавать постоянные профили и управлять ими через встроенное текстовое меню.

> Текущая версия: **0.3.6**

## ✨ Основные возможности
1. **Быстрая изолированная сессия** — Chrome стартует с уникальной временной папкой и автоматически очищается при закрытии.
2. **Гибкий выбор URL** — можно вводить ссылку вручную, брать её из закладок или из списка недавних запусков.
3. **Менеджер профилей**:
   - создание именованных профилей с собственным хранилищем;
   - запуск профиля с сохранённым или кастомным URL;
   - полное удаление профиля или очистка только данных браузера;
   - просмотр списка профилей с их стартовыми адресами.
4. **Закладки и «Недавние»** — сохранение полезных URL и быстрый повторный запуск в один клик.
5. **Полная изоляция данных** — отдельные cookie/кэш/история для каждой сессии и профиля с возможностью полной очистки.
6. **Портативность** — достаточно скачать `isochrome.bat`, дополнительных установок не требуется.
7. **Темизация интерфейса** — три консольные темы (тёмная, светлая, неоновая) с сохранением выбора.

## Как пользоваться
1. Скачайте `isochrome.bat` и запустите двойным кликом (права администратора не нужны).
2. В главном меню доступны действия:
   - `1` — **Быстрая сессия**: создаётся временный профиль в `%TEMP%`, Chrome открывается в отдельном окне. После закрытия окна данные удаляются автоматически.
   - `2` — **Создание профиля**: задайте имя и при желании стартовый URL, чтобы хранить отдельный рабочий контекст.
   - `3` — **Запуск профиля**: выберите профиль и URL (из закладок, последних запусков или вручную) и откройте Chrome с нужными данными.
   - `4` — **Очистка/удаление профиля**: полностью удалите профиль или очистите только данные браузера.
   - `5` — **Список профилей**: посмотрите, какие рабочие окружения доступны.
   - `6` — **Закладки**: просматривайте, добавляйте и удаляйте важные URL.
   - `7` — **Темы интерфейса**: переключайте цветовую схему консоли и сохраняйте её.
3. В режиме быстрой сессии придерживайтесь подсказок в консоли: работайте в открывшемся окне Chrome и закройте его, когда захотите завершить временную среду.
4. Для пакетного редактирования закладок используйте `bookmarks.txt` рядом со скриптом (каждая строка: `Название|URL`). Строки, начинающиеся с `#`, игнорируются.
5. Скрипт автоматически ищет Chrome в `Program Files`, `Program Files (x86)` и `%LocalAppData%`. Если браузер не найден, выводится чёткое сообщение с рекомендациями.

## Технические детали
- Для временных сессий используется `%TEMP%` с флагом `--user-data-dir`.
- Постоянные профили хранятся в `profiles/<имя>/data`, конфигурация — в `profile.cfg`.
- Все действия сопровождаются логами с префиксом `[IsoChrome]`.

## 📁 Структура файлов
- `isochrome.bat` — основной сценарий, который запускает меню и все операции.
- `profiles/` — каталог с постоянными профилями; служебная папка `_quick` содержит «Недавние» (`recent.log`) и временные данные.
- `bookmarks.txt` — список закладок в формате `Название|URL`; строки, начинающиеся с `#`, игнорируются.
- `changelog.md` — журнал версий; каждая новая запись добавляется сверху.

## ⚠️ Требования
- Windows 10/11
- Установленный Google Chrome

## 📝 Лицензия
Проект распространяется под лицензией [MIT](LICENSE). Можно свободно изменять и использовать в любых целях.
