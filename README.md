# IsoChrome Lite IDE 🚀

**IsoChrome Lite IDE** — сверхлегкий менеджер изолированных сессий Google Chrome на Batch. Скрипт запускает «чистые» окна браузера, умеет создавать постоянные профили и управлять ими через встроенное текстовое меню.

> Текущая версия: **0.0.5**

## ✨ Основные возможности
1. **Быстрая изолированная сессия** — Chrome стартует с уникальной временной папкой и автоматически очищается при закрытии.
2. **Гибкий выбор URL** — можно вводить ссылку вручную, брать её из закладок или из списка недавних запусков.
3. **Менеджер профилей**:
   - создание именованных профилей с собственным хранилищем;
   - запуск профиля с сохранённым или кастомным URL;
   - полное удаление профиля или очистка только данных браузера;
   - просмотр списка профилей с их стартовыми адресами.
4. **Закладки и «Недавние»** — сохранение полезных URL и быстрый повторный запуск в один клик.
5. **Полная изоляция данных** — отдельные cookie/кэш/история для каждой сессии и профиля с возможностью полной очистки.
6. **Портативность** — достаточно скачать `isochrome.bat`, дополнительных установок не требуется.

## 🚀 Как пользоваться
1. Скачайте/создайте `isochrome.bat` и запустите двойным кликом.
2. В главном меню выберите нужное действие:
   - `1` — мгновенная временная сессия (без остаточных данных).
   - `2` — создание постоянного профиля и сохранение стартового URL.
   - `3` — запуск сохранённого профиля; URL можно выбрать из закладок/недавних запусков или ввести вручную.
   - `4` — управление профилем: удаление целиком или только очистка данных Chrome.
   - `5` — вывод списка профилей.
   - `6` — управление закладками (просмотр, добавление, удаление).
3. Для массового редактирования закладок используйте файл `bookmarks.txt` рядом со скриптом (каждая строка: `Название|URL`).
4. Скрипт сам найдёт Chrome (`Program Files`, `Program Files (x86)`, `%LocalAppData%`). Если браузер не обнаружен, появится понятная ошибка.

## 🛠 Технические детали
- Для временных сессий используется `%TEMP%` с флагом `--user-data-dir`.
- Постоянные профили хранятся в `profiles/<имя>/data`, конфигурация — в `profile.cfg`.
- Все действия сопровождаются логами с префиксом `[IsoChrome]`.

## ⚠️ Требования
- Windows 10/11
- Установленный Google Chrome

## 📝 Лицензия
Проект распространяется под лицензией [MIT](LICENSE). Можно свободно изменять и использовать в любых целях.
